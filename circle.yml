machine:
  java:
    version: openjdk8

  services:
    - docker

  environment:
    NXF_VER: 0.24.2

dependencies:
  override:
  - curl -fsSL get.nextflow.io | bash
  - chmod +x nextflow
  - sudo mv nextflow /usr/local/bin/

test:
  post:
    - ./script/test_docker.sh
