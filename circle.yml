machine:
  java:
    version: openjdk8

  services:
    - docker

  environment:
    NXF_VER: 0.25.7

dependencies:
  override:
  - curl -fsSL get.nextflow.io | bash
  - chmod +x nextflow
  - sudo mv nextflow /usr/local/bin/

test:
  post:
    - ./scripts/test.sh
