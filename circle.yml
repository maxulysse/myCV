machine:
  java:
    version: openjdk7

  services:
    - docker

  environment:
    NXF_VER: 0.23.0

dependencies:
  override:
  - docker pull maxulysse/mycv:v0.16.1221
  - curl -fsSL get.nextflow.io | bash

test:
  post:
    - ./nextflow run . -with-docker -profile docker
