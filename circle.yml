machine:
  java:
    version: openjdk8

  services:
    - docker

  environment:
    NXF_VER: 0.25.7

dependencies:
  override:
  - ./script/install.sh --tool nextflow

test:
  post:
    - ./scripts/test.sh
