/*
vim: syntax=groovy
-*- mode: groovy;-*-
 * -------------------------------------------------
 * Nextflow config file
 * -------------------------------------------------
 * Define container
 */

 process.container = "${params.repository}/mycv:${params.tag}"
