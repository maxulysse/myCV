sudo: required

language: java

jdk: openjdk8

services:
  - docker

env:
  - NXF_VER=0.25.6 PROFILE=docker
  - NXF_VER=0.25.6 SGT_VER=2.3.1 PROFILE=singularity

install: # Install Nextflow (and Singularity if needed)
  - "./scripts/install.sh --tool $TOOL_INSTALL"

script:
  - "./scripts/test.sh --profile $PROFILE"
